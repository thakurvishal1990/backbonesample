define([
  'lodash',
  'backbone'
], function(_, Backbone) {
  var projectsModel = Backbone.Model.extend({
    defaults: {
      
    },
    
    initialize: function(){
      
    }
    
  });
  return projectsModel;

});